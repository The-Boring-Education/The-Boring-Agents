🧠 DSA Interview Questions – The Boring Education

📋 Questions List

1. What is the time complexity of nested loops with varying ranges?
2. Derive the time complexity of a recursive Fibonacci function and optimize it.
3. Analyze the space complexity of Merge Sort.
4. Compare the time complexities of Linear Search vs. Binary Search with real use cases.
5. For an algorithm involving nested recursive calls and loops, how do you calculate the overall time and space complexity?

6. Write a function to reverse a string without using built-in functions.
7. Compute factorial of a number iteratively and recursively.
8. Generate the first n Fibonacci numbers.
9. Check if a number is a palindrome using mathematical operations.
10. Find the Nth Fibonacci number using matrix exponentiation.

11. Find the maximum sum of a subarray of size K.
12. Find the longest substring with at most K distinct characters.
13. Find the minimum length subarray with a sum ≥ target.
14. Find the longest substring without repeating characters.
15. Count how many anagrams of a word exist in a string.

16. Check if an array has two numbers that sum to a target value.
17. Reverse an array using two pointers.
18. Merge two sorted arrays in-place without extra space.
19. Remove duplicates from a sorted array.
20. Solve the trapping rain water problem.

21. Fast and Slow Pointers
22. Detect a cycle in a linked list using Floyd’s cycle detection.
23. Find the middle node of a singly linked list.
24. Check if a linked list is a palindrome.
25. Determine the length of the loop in a linked list with cycle.
26. Determine if a number is a “happy number”.

27. Move all zeros to the end of an array while maintaining order.
28. Check if two strings are anagrams.
29. Rotate an array to the right by k steps.
30. Group anagrams from a list of strings.
31. Return product of array except self without using division.

32. Implement bubble sort and analyze its complexity.
33. Perform binary search on a sorted array.
34. Search for a target in a rotated sorted array.
35. Find the Kth largest element using quickselect.
36. Find the median of two sorted arrays.

37. Print all subsets of a string.
38. Write a recursive function to compute power (x^n).
39. Generate all combinations of balanced parentheses.
40. Count all unique paths in a N x N maze.
41. Solve a Sudoku puzzle using recursion.

42. Reverse a linked list iteratively.
43. Detect and remove a loop in a linked list.
44. Merge two sorted linked lists.
45. Add two numbers represented by linked lists.
46. Deep copy a linked list with random pointers.

47. Implement a stack using an array.
48. Check if a string has balanced parentheses.
49. Find the next greater element for each element in an array.
50. Design an LRU cache using doubly linked list and hashmap.
51. Evaluate a postfix (Reverse Polish) expression.

52. Perform inorder traversal of a binary tree recursively.
53. Find the height of a binary tree.
54. Check if a binary tree is height-balanced.
55. Find the lowest common ancestor in a BST.
56. Serialize and deserialize a binary tree.

57. Implement a min heap from scratch.
58. Find the Kth largest element in an array using heap.
59. Merge k sorted linked lists.
60. Find the top K frequent elements in a list.
61. Design a data structure to continuously return the median.

62. Hashing and Hash Tables
63. Find the first non-repeating character in a string.
64. Solve the Two Sum problem.
65. Find the length of the longest subarray with sum = 0.
66. Count subarrays with equal number of 0s and 1s.
67. Count the number of subarrays that sum to a given value K.

68. Climbing stairs with 1 or 2 steps at a time.
69. Minimum cost to reach the end of a cost array.
70. House robber problem with non-adjacent selections.
71. Find the longest increasing subsequence.
72. Solve the 0/1 knapsack problem.

73. Represent a graph using adjacency list.
74. Perform BFS and DFS traversals.
75. Detect cycle in an undirected graph using DFS.
76. Perform topological sort using Kahn’s Algorithm.
77. Find shortest paths from source using Dijkstra’s Algorithm.

78. Select the maximum number of non-overlapping activities.
79. Solve the fractional knapsack problem.
80. Find the minimum number of platforms required at a railway station.
81. Job scheduling to maximize profit.
82. Build Huffman tree and calculate minimum encoding length.

83. Check if a number is a power of 2.
84. Count number of 1s in binary representation of a number.
85. Find the single number in an array where others appear twice.
86. Find XOR of numbers from 1 to N.
87. Divide two integers without using /, \*, or %.

88. Implement binary search using recursion.
89. Implement merge sort recursively.
90. Implement quick sort recursively.
91. Count the number of inversions in an array.
92. Find the closest pair of points in 2D plane.

93. Solve the N-Queens problem (N=4).
94. Rat in a maze – find all paths.
95. Word search in a 2D character grid.
96. Solve subset sum problem using backtracking.
97. Solve a cryptarithmetic puzzle (SEND + MORE = MONEY).

⸻

📝 Instructions for Answer Generation
• Provide detailed explanations with code examples (in Python, Java, or C++)
• Include best practices and common pitfalls
• Add real-world scenarios and use cases
• Focus on practical implementation and optimization strategies
• Mention time and space complexities
• Provide debugging tips and tricks for each problem
